#pragma once
// Copy this file to include/secrets.h and set your own values.
// secrets.h is gitignored — real credentials never enter the repo.
// These are only the FACTORY DEFAULTS for first boot; both can be changed
// at runtime from the web panel (stored in NVS afterwards).

#define TT_AP_PASSWORD "change-me-8chars"   // device hotspot password (min 8 chars)
#define TT_WEB_PASSWORD "change-me"         // web panel password (user: admin, min 4 chars)
